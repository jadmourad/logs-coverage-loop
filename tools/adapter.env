# =============================================================================
# Swap point for the real monitoring tool.
#
# STANDALONE_CMD is invoked as:
#   $STANDALONE_CMD --logs-root DIR --config FILE --out DIR \
#                   [--max-unmatched-lines N] [--label STR]
#
# It must produce <out>/coverage-report.json and <out>/uncovered/... exactly as
# described in docs/CONTRACTS.md.
#
# ---- FILLER (current) -------------------------------------------------------
# A working fake that implements the contract against config/logs-parsing-config.yml.
# It is a real scanner, not a stub: the whole loop can be exercised end to end today.
STANDALONE_CMD="python3 $HERE/fake/fake_standalone.py"

# ---- REAL TOOL (examples -- uncomment and adapt one) ------------------------
# Direct, if the real standalone mode already speaks the contract:
# STANDALONE_CMD="/opt/monitoring/bin/monitor --standalone"
#
# Via a jar:
# STANDALONE_CMD="java -jar /opt/monitoring/monitor.jar standalone"
#
# Via a translation wrapper you write, if the real output shape differs:
# STANDALONE_CMD="python3 $HERE/real/adapt_real_tool.py"
# =============================================================================
